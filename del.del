read -p "输入domain:" DOmain
if   [[ -z "$DOmain" ]]; then
echo "必须输入 domain"
read -p "输入domain:" DOmain
if   [[ -z "$DOmain" ]]; then
echo "必须输入 domain"
read -p "输入domain:" DOmain
if   [[ -z "$DOmain" ]]; then
echo "必须输入 domain"
read -p "输入domain:" DOmain
if   [[ -z "$DOmain" ]]; then
echo "必须输入 domain"
read -p "输入domain:" DOmain
if   [[ -z "$DOmain" ]]; then
echo "必须输入 domain"
read -p "输入domain:" DOmain
if   [[ -z "$DOmain" ]]; then
exit 1
fi
